on local server:
    if using xampp:
        in logic.php
        $host = "localhost";
        $user = "root";
        $password = "";
        $dbname = "your_database"; 

change database table names accordingly in create.php, index.php and view.php